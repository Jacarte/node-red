
const target = { };
const handler1  = {
    get(target, prop, receiver) {
        return () => {
            console.log("Not implemented", prop);
            return new Proxy(target, handler1)
        }
    },
  };
  

const proxy1 = new Proxy(target, handler1);

module.exports = proxy1;