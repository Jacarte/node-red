
let msg = Node.IO.msg();
let node =  { };  // Node.IO.node();

const send = Node.IO.send;
const done = Node.IO.done;

node.send = send;
node.done = done


/*RED._ = function(obj) {
  return obj;
}*/

{{{
  inner_code
}}}



function main() {
  {{mm}}({{{args}}});
}

main()